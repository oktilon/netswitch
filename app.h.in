#define ETH_INTERFACE   "@eth_ifn@"
#define MOB_INTERFACE   "@wan_ifn@"

#define MIN(a,b)        ((a<b) ? (a) : (b))

#define VERSION         "@version@"

void logger(const char *format, ...);
void elogger(const char *format, ...);